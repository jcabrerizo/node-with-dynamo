{
  "name": "my-node",
  "version": "1.0.0",
  "description": "[source] ---- npm init npm install express --save npm install aws-sdk --save ----",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1445.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
